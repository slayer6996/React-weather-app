# React-weather-app
This is a weather app created with react.js and OpenWeatherMap API

### To run the build
Run this in the CLI in the directory of the files
```
npm start
```

## Note:
in src/components/main.js line 11, 'apiKey' is not defined.
To generate an API key go to https://openweathermap.org/api
